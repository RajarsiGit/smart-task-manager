# Neon Database Connection String
# Get this from your Neon console: https://console.neon.tech
# Format: postgres://[user]:[password]@[neon_hostname]/[database]?sslmode=require
DATABASE_URL=

# Frontend API Base URL (optional - defaults to /api)
# For local development, you might use: http://localhost:3000/api
VITE_API_BASE_URL=/api

# Enable API calls (set to 'false' to use localStorage only)
# Set to 'true' or omit to use API
VITE_USE_API=true
